#include "MyGame.h"

//After creating an object of the class "MyGame", the object "game" accesses the method jugar(), which initializes the game.
int main(){
    MyGame game;
    game.jugar();

    return 0;
}
